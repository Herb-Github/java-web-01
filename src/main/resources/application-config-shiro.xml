<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns:util="http://www.springframework.org/schema/util"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd"
       default-lazy-init="true">


    <description>Shiro Config</description>

    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>

    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager"/>
        <property name="loginUrl" value="${cas.client.login.url}"/>
        <property name="successUrl" value="/main"/>
        <property name="unauthorizedUrl" value="${cas.client.login.url}"/>

        <property name="filters">
            <util:map>
                <entry key="authc">
                    <bean class="org.apache.shiro.web.filter.authc.PassThruAuthenticationFilter"/>
                </entry>
                <entry key="casFilter" value-ref="casFilter" />
            </util:map>
        </property>
        <property name="filterChainDefinitions">
            <value>
                # some chain definitions:
                /img/** = anon
                /js/** = anon
                /css/** = anon
                /captcha = anon
                /logoutsub = anon
                /failure = anon
                <!--/oapi/** = ipFilter  用于检查IP是否合法 -->
                <!--/app/** = appFilter  用于移动端登录 -->

                # cas 用于 CAS 单点登录
                /login = casFilter
                /** = authc
            </value>
        </property>
    </bean>

    <bean id="casFilter" class="org.apache.shiro.cas.CasFilter">
        <property name="failureUrl" value="${cas.client.failure.url}"/>
    </bean>

    <!-- Shiro's main business-tier object for web-enabled applications -->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <!-- Single realm app. If you have multiple realms, use the 'realms' property instead. -->
        <property name="realm" ref="myCasRealm"/>
        <!--<property name="cacheManager" ref="cacheManager"/>
        <property name="sessionMode" value="native"/> -->
    </bean>

    <bean id="databaseRealm" class="com.joywifi.lottery.command.security.DatabaseRealm"/>


    <!-- 如果要实现cas的remember me的功能，需要用到下面这个bean，并设置到securityManager的subjectFactory中 -->
    <bean id="casSubjectFactory" class="org.apache.shiro.cas.CasSubjectFactory"/>

    <!-- 如果要实现 cas 的 remember me 功能，需要用到 casSubjectFactory -->
    <bean id="securityManagerCas" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="myCasRealm"/>
        <property name="subjectFactory" ref="casSubjectFactory"/>
    </bean>

    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="staticMethod" value="org.apache.shiro.SecurityUtils.setSecurityManager"/>
        <property name="arguments" ref="securityManagerCas"/>
    </bean>

</beans>
